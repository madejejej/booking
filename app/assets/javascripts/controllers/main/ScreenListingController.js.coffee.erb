@controllers.controller( 'ScreenListingController',['$scope', '$routeParams', '$location', 'ScreenService',  ($scope, $routeParams, $location, ScreenService) ->

    $scope.cinemaId = $routeParams.cinema_id
    $scope.screens = ScreenService.getCinemaScreens($scope.cinemaId)

    $scope.openAddScreenDialog = () ->
         #   modalInstance = $modal.open
         #   templateUrl: '<%= asset_path("cinemas/addScreenDialog.html") %>',
         #   controller: 'AddScreenController'
         #   resolve:
         #       screenId: () ->
        console.log("openAddScreenDialog")
])
